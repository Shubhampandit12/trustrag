# Full runtime stack (GPU box / A100 run). Install with: pip install -r requirements.txt
# --- eval core (also in requirements-ci.txt) ---
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
pydantic>=2.5
pyyaml>=6.0
joblib>=1.3
# --- retrieval / generation ---
torch>=2.1
transformers>=4.40
sentence-transformers>=2.7      # bge-base-en-v1.5 + bge-reranker-base
faiss-cpu>=1.7.4                # per-question in-memory IndexFlatIP
trafilatura>=1.8                # HTML -> text (BS4 fallback)
beautifulsoup4>=4.12
# generation backend is reached over HTTP (vLLM OpenAI-compat) via LLM_BASE_URL
openai>=1.30                    # OpenAI-compatible client for vLLM / judge API
# --- service / demo / tracking ---
fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=4.0
wandb>=0.16
matplotlib>=3.7                 # risk-coverage / reliability plots
python-dotenv>=1.0
