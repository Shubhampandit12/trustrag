# Light stack: the eval core (scorer, metrics, gate, signals) with NO torch/vllm/faiss.
# This is enough to run the unit tests and the synthetic end-to-end demo.
# Install with: pip install -r requirements-ci.txt
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
pydantic>=2.5
pyyaml>=6.0
joblib>=1.3
pytest>=7
